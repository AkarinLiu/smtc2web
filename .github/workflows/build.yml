name: Build

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  Build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'windows-latest'
            target: 'x86_64-pc-windows-msvc'
            arch: 'x64'
          - platform: 'windows-latest'
            target: 'aarch64-pc-windows-msvc'
            arch: 'arm64'

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: ${{ github.ref }}
          submodules: true
      
      - name: Setup Rust and target
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24'
          cache: 'yarn'
          install-command: 'yarn install --frozen-lockfile'

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-
          workspaces: |
            src-tauri
          cache-directories: |
            src-tauri/target
          cache-targets: true

      - name: Build the app
        working-directory: src-tauri
        run: |
          yarn global add @tauri-apps/cli
          yarn tauri build --target ${{ matrix.target }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.platform }}-${{ matrix.arch }}
          path: |
            src-tauri/target/${{ matrix.target }}/release/bundle/nsis/*.exe
          if-no-files-found: error

  Release:
    needs: Build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: Create GitHub Release
        id: create-release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          draft: false
          prerelease: false
          body: |
            # ${{ github.ref_name }}

            ## 更新日志
            请查看 [Changelog.md](https://github.com/${{ github.repository }}/blob/${{ github.ref }}/Changelog.md) 获取详细更新信息。
          files: |
            ./artifacts/**/*.exe
            ./artifacts/**/*.dmg
            ./artifacts/**/*.AppImage

  Scan:
    needs: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 修改这里：只下载制品，不指定具体文件
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      # 添加调试步骤查看文件结构
      - name: List downloaded files
        run: |
          find ./artifacts -type f -name "*.exe" -o -name "*.dmg" -o -name "*.AppImage" | head -20
          echo "Total files found:"
          find ./artifacts -type f -name "*.exe" -o -name "*.dmg" -o -name "*.AppImage" | wc -l

      - name: VirusTotal Scan
        uses: cssnr/virustotal-action@v1
        with:
          vt_api_key: ${{ secrets.VT_API_KEY }}
          # 使用正确的通配符路径
          file_globs: |
            ./artifacts/**/*.exe
          rate_limit: 2
          update_release: true
          release_heading: '## VirusTotal 扫描结果'
          release_id: ${{ github.ref_name }}
          github_token: ${{ secrets.GITHUB_TOKEN }}